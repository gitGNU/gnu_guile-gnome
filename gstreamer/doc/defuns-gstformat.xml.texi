
@c %start of fragment

@deffn Function gst-format-get-name  (self@tie{}@code{<gst-format*>}) @result{}@tie{} (ret@tie{}@code{mchars})
Get a printable name for the given format. Do not modify or free.

@table @var
@item format
a @code{<gst-format>}

@item ret
a reference to the static name of the format or NULL if the format is unknown.

@end table

@end deffn

@deffn Function gst-format-to-quark  (self@tie{}@code{<gst-format*>}) @result{}@tie{} (ret@tie{}@code{unsigned-int})
Get the unique quark for the given format.

@table @var
@item format
a @code{<gst-format>}

@item ret
the quark associated with the format or 0 if the format is unknown.

@end table

@end deffn

@deffn Function gst-format-register  (nick@tie{}@code{mchars}) (description@tie{}@code{mchars}) @result{}@tie{} (ret@tie{}@code{<gst-format>})
Create a new GstFormat based on the nick or return an already registered format
with that nick.

@table @var
@item nick
The nick of the new format

@item description
The description of the new format

@item ret
A new GstFormat or an already registered format with the same nick. MT safe.

@end table

@end deffn

@deffn Function gst-format-get-by-nick  (nick@tie{}@code{mchars}) @result{}@tie{} (ret@tie{}@code{<gst-format>})
Return the format registered with the given nick.

@table @var
@item nick
The nick of the format

@item ret
The format with @var{nick} or GST_FORMAT_UNDEFINED if the format was not
registered.

@end table

@end deffn

@deffn Function gst-formats-contains  (self@tie{}@code{<gst-format*>}) (format@tie{}@code{<gst-format>}) @result{}@tie{} (ret@tie{}@code{bool})
See if the given format is inside the format array.

@table @var
@item formats
The format array to search

@item format
the format to find

@item ret
TRUE if the format is found inside the array

@end table

@end deffn

@deffn Function gst-format-get-details  (format@tie{}@code{<gst-format>}) @result{}@tie{} (ret@tie{}@code{<gst-format-definition*>})
Get details about the given format.

@table @var
@item format
The format to get details of

@item ret
The @code{<gst-format-definition>} for @var{format} or NULL on failure. MT safe.

@end table

@end deffn

@deffn Function gst-format-iterate-definitions  @result{}@tie{} (ret@tie{}@code{<gst-iterator*>})
Iterate all the registered formats. The format definition is read only.

@table @var
@item ret
A GstIterator of @code{<gst-format-definition>}.

@end table

@end deffn


@c %end of fragment
