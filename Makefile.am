# this file is generated by autogen-pkg.sh

SUBDIRS = clutter doc examples tests
EXTRA_DIST = dev-environ.in h2def.py autogen.sh HACKING

dist-gzip: distdir
	$(AMTAR) --format=ustar -chf - $(distdir) | GZIP=$(GZIP_ENV) gzip -c >$(distdir).tar.gz
	$(am__remove_distdir)
dist dist-all: distdir
	$(AMTAR) --format=ustar -chf - $(distdir) | GZIP=$(GZIP_ENV) gzip -c >$(distdir).tar.gz
	$(am__remove_distdir)
