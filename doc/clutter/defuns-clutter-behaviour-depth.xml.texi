
@c %start of fragment

@deffn Function clutter-behaviour-depth-new  (alpha@tie{}@code{<clutter-alpha>}) (depth_start@tie{}@code{int}) (depth_end@tie{}@code{int}) @result{}@tie{} (ret@tie{}@code{<clutter-behaviour>})
@samp{clutter_behaviour_depth_new} is deprecated and should not be used
in newly-written code. 1.6

Creates a new @code{<clutter-behaviour-depth>} which can be used to
control the ClutterActor:depth property of a set of
@code{<clutter-actor>}s.

If @var{alpha} is not @samp{@code{#f}}, the @code{<clutter-behaviour>}
will take ownership of the @code{<clutter-alpha>} instance. In the case
when @var{alpha} is @samp{@code{#f}}, it can be set later with
@code{clutter-behaviour-set-alpha}.

@table @var
@item alpha
a @code{<clutter-alpha>} instance, or @samp{@code{#f}}.

@item depth-start
initial value of the depth

@item depth-end
final value of the depth

@item ret
the newly created behaviour.

@end table

Since 0.4

@end deffn

@deffn Function clutter-behaviour-depth-set-bounds  (self@tie{}@code{<clutter-behaviour-depth>}) (depth_start@tie{}@code{int}) (depth_end@tie{}@code{int})
@deffnx Method set-bounds
@samp{clutter_behaviour_depth_set_bounds} is deprecated and should not
be used in newly-written code. 1.6

Sets the boundaries of the @var{behaviour}.

@table @var
@item behaviour
a @code{<clutter-behaviour-depth>}

@item depth-start
initial value of the depth

@item depth-end
final value of the depth

@end table

Since 0.6

@end deffn

@deffn Function clutter-behaviour-depth-get-bounds  (self@tie{}@code{<clutter-behaviour-depth>}) @result{}@tie{} (depth_start@tie{}@code{int}) (depth_end@tie{}@code{int})
@deffnx Method get-bounds
@samp{clutter_behaviour_depth_get_bounds} is deprecated and should not
be used in newly-written code. 1.6

Gets the boundaries of the @var{behaviour}

@table @var
@item behaviour
a @code{<clutter-behaviour-depth>}

@item depth-start
return location for the initial depth value, or @samp{@code{#f}}.

@item depth-end
return location for the final depth value, or @samp{@code{#f}}.

@end table

Since 0.6

@end deffn


@c %end of fragment
