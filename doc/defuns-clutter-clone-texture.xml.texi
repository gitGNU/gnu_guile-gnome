
@c %start of fragment

@deftp Class <clutter-clone-texture>
Derives from @code{<clutter-actor>}.

This class defines the following slots:

@table @code
@item parent-texture
The parent texture to clone

@item repeat-y
Reapeat underlying pixbuf rather than scale in y direction.

@item repeat-x
Reapeat underlying pixbuf rather than scale in x direction.

@end table

@end deftp

@deffn Function clutter-clone-texture-new  (texture@tie{}@code{<clutter-texture>}) @result{}@tie{} (ret@tie{}@code{<clutter-actor>})
Creates an efficient 'clone' of a pre-existing texture with which it shares the
underlying pixbuf data.

You can use @code{clutter-clone-texture-set-parent-texture} to change the cloned
texture.

@table @var
@item texture
a @code{<clutter-texture>}, or @samp{@code{#f}}

@item ret
the newly created @code{<clutter-clone-texture>}

@end table

@end deffn


@c %end of fragment
