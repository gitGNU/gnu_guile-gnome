
@c %start of fragment

@deffn Function clutter-behaviour-scale-new  (alpha@tie{}@code{<clutter-alpha>}) (x_scale_start@tie{}@code{double}) (y_scale_start@tie{}@code{double}) (x_scale_end@tie{}@code{double}) (y_scale_end@tie{}@code{double}) @result{}@tie{} (ret@tie{}@code{<clutter-behaviour>})
@samp{clutter_behaviour_scale_new} is deprecated and should not be used
in newly-written code. 1.6

Creates a new @code{<clutter-behaviour-scale>} instance.

If @var{alpha} is not @samp{@code{#f}}, the @code{<clutter-behaviour>}
will take ownership of the @code{<clutter-alpha>} instance. In the case
when @var{alpha} is @samp{@code{#f}}, it can be set later with
@code{clutter-behaviour-set-alpha}.

@table @var
@item alpha
a @code{<clutter-alpha>} instance, or @samp{@code{#f}}.

@item x-scale-start
initial scale factor on the X axis

@item y-scale-start
initial scale factor on the Y axis

@item x-scale-end
final scale factor on the X axis

@item y-scale-end
final scale factor on the Y axis

@item ret
the newly created @code{<clutter-behaviour-scale>}.

@end table

Since 0.2

@end deffn

@deffn Function clutter-behaviour-scale-set-bounds  (self@tie{}@code{<clutter-behaviour-scale>}) (x_scale_start@tie{}@code{double}) (y_scale_start@tie{}@code{double}) (x_scale_end@tie{}@code{double}) (y_scale_end@tie{}@code{double})
@deffnx Method set-bounds
@samp{clutter_behaviour_scale_set_bounds} is deprecated and should not
be used in newly-written code. 1.6

Sets the bounds used by scale behaviour.

@table @var
@item scale
a @code{<clutter-behaviour-scale>}

@item x-scale-start
initial scale factor on the X axis

@item y-scale-start
initial scale factor on the Y axis

@item x-scale-end
final scale factor on the X axis

@item y-scale-end
final scale factor on the Y axis

@end table

Since 0.6

@end deffn

@deffn Function clutter-behaviour-scale-get-bounds  (self@tie{}@code{<clutter-behaviour-scale>}) @result{}@tie{} (x_scale_start@tie{}@code{double}) (y_scale_start@tie{}@code{double}) (x_scale_end@tie{}@code{double}) (y_scale_end@tie{}@code{double})
@deffnx Method get-bounds
@samp{clutter_behaviour_scale_get_bounds} is deprecated and should not
be used in newly-written code. 1.6

Retrieves the bounds used by scale behaviour.

@table @var
@item scale
a @code{<clutter-behaviour-scale>}

@item x-scale-start
return location for the initial scale factor on the X axis, or
@samp{@code{#f}}.

@item y-scale-start
return location for the initial scale factor on the Y axis, or
@samp{@code{#f}}.

@item x-scale-end
return location for the final scale factor on the X axis, or
@samp{@code{#f}}.

@item y-scale-end
return location for the final scale factor on the Y axis, or
@samp{@code{#f}}.

@end table

Since 0.4

@end deffn


@c %end of fragment
